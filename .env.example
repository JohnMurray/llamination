# Copy this file to .env only when the defaults need to be overridden locally.
LLAMINATION_POSTGRES_DB=llamination
LLAMINATION_POSTGRES_USER=llamination
LLAMINATION_POSTGRES_PASSWORD=llamination
LLAMINATION_POSTGRES_PORT=5432
LLAMINATION_REDIS_PORT=6379

